import random
# Split string method
names_string = input("Give me everybody's names, separated by a comma. ")
names = names_string.split(", ")
# 🚨 Don't change the code above 👆

#Write your code below this line 👇
number_of_persons=int(len(names))
#selected=random.randint(1,number_of_persons)
print(f"{names[random.randint(0,number_of_persons)]} is going to buy the meal today!")
#print(selected)
#print(number_of_persons)
#print(names[0])
